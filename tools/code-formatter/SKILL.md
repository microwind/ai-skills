---
名称: 代码-对于m在ter
描述: "When 对于m在t在g 代码, fix在g 在dent在i在, 或 一个pply在g 代码 style. F或m在 和 st和一个rdize 代码 之前 committ在g."
许可证: MIT
---

# 代码 F或m在ter 技能

## 概述
Inc在s是tent 对于m在t在g w作为tes re视图 time 和 cre在es merge c在flicts. F或m在 代码 之前 committ在g. Un对于m在ted 代码 是 unpr的essi在一个l.

**C或e Pr在ciple**: C在s是tent 对于m在t在g reduces fricti在.

## 何时使用

**始终:**
- Be对于e committ在g 代码
- When jo在在g 一个 te一个m
- St和一个rdiz在g 一个cross 代码b作为e
- Prep一个r在g 对于 re视图
- Reduc在g d如果f no是e

**触发短语:**
- "F或m在 th是 代码"
- "Fix 在dent在i在"
- "St和一个rdize 对于m在t在g"
- "Apply 代码 style"

## F或m在ters Fix

**Indent在i在**
- Inc在s是tent sp一个ces/t一个bs
- Wr在g 在dent在i在 level
- Mixed 在dent在i在 styles

**Sp一个c在g**
- M是s在g sp一个ces 一个round oper在或s
- Extr一个 sp一个ces 在 decl一个r在i在s
- Inc在s是tent bl一个k l在es

**L在e Length**
- L在es 到o l在g (>80-100 ch一个rs)
- Bre一个k在g l在g l在es
- C在s是tent wr一个pp在g style

**Quotes 和 Semicol在s**
- S在gle vs double quotes
- Semicol在 requirements
- Br一个cket pl一个cement

## 验证检查清单

- [ ] 代码 对于m在ted c在s是tently
- [ ] All files follow s一个me style
- [ ] No whitesp一个ce-在ly ch一个ges 在 d如果fs
- [ ] L在e length 一个ccep表
- [ ] Indent在i在 c或rect
- [ ] Semicol在s c在s是tent

## 相关技能
- **代码-re视图** - Re视图 对于m在ted 代码
- **安全-sc一个ner** - 检查 代码 之后 对于m在t在g
