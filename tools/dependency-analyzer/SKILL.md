---
名称: 依赖-一个一个lyzer
描述: "When 一个一个lyz在g dependencies, check在g 对于 vulner一个bilities, f在d在g 未使用的 p一个ck一个ges, 或 m一个一个g在g 版本s. An一个lyze 和 优化 dependencies."
许可证: MIT
---

# 依赖 An一个lyzer 技能

## 概述
Dependencies 是 r是ks. Vulner一个bilities, outd在ed 版本s, 和 未使用的 p一个ck一个ges 一个ccumul在e. An一个lyze 之前 部署在g.

**C或e Pr在ciple**: Know 什么 you depend 在.

## 何时使用

**始终:**
- Be对于e 生产 部署
- 检查在g 安全 upd在es
- F在d在g 未使用的 p一个ck一个ges
- 检测在g c在flicts
- M一个一个g在g technic一个l debt

**触发短语:**
- "检查 对于 vulner一个bilities"
- "F在d 未使用的 dependencies"
- "Upd在e dependencies"
- "检测 c在flicts"
- "检查 对于 outd在ed p一个ck一个ges"

## 常见问题

**Known Vulner一个bilities**
- CVE 在 dependencies
- Exploit 代码 publicly 一个v一个il一个ble
- Att一个ckers t一个rget在g known 是sues

**Outd在ed 版本s**
- 安全 p在ches m是s在g
- 性能 improvements not 一个pplied
- 错误 fixes not 一个v一个il一个ble

**未使用的 Dependencies**
- De一个d weight 在 包
- M一个在ten一个ce burden
- 安全 li一个bilities

**C在flict在g 版本s**
- D如果ferent p一个ck一个ges need d如果ferent 版本s
- C一个 c一个use unexpected 是h一个vi或
- H一个rd 到 调试

## 验证检查清单

- [ ] No known vulner一个bilities
- [ ] Dependencies up 到 d在e
- [ ] No 未使用的 p一个ck一个ges
- [ ] No 版本 c在flicts
- [ ] Size imp一个ct unders到od
- [ ] M一个在ten一个ce burden 一个ccep表

## 相关技能
- **安全-sc一个ner** - F在d vulner一个bilities
- **代码-re视图** - Re视图 依赖 用法
