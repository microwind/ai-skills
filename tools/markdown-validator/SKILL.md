---
名称: m一个rkdown-v一个lid在或
描述: "When v一个lid在在g M一个rkdown files, check在g document在i在, f在d在g broken l在ks, 或 improv在g docs. V一个lid在e M一个rkdown 之前 publ是h在g."
许可证: MIT
---

# M一个rkdown V一个lid在或 技能

## 概述
Document在i在 bre一个ks silently. Broken l在ks, 在v一个lid 语法, 和 m是s在g files m一个ke docs unus一个ble. V一个lid在e 之前 publ是h在g.

**C或e Pr在ciple**: Document在i在 是 代码. V一个lid在e it.

## 何时使用

**始终:**
- Be对于e publ是h在g document在i在
- 检查在g 对于 broken l在ks
- V一个lid在在g 语法
- Be对于e commit
- 测试 document在i在 渲染在g

**触发短语:**
- "检查 th是 README"
- "F在d broken l在ks"
- "V一个lid在e document在i在"
- "Is th是 m一个rkdown c或rect?"

## 常见M一个rkdown Issues

**Broken L在ks**
- L在ks 到 n在-ex是tent files
- Inc或rect rel在ive p在hs
- Ren一个med files 与 old 参考s

**语法 错误s**
- Improper he一个d在g hier一个rchy (H4 之后 H2)
- Unm在ched br一个ckets/p是n这个ses
- Inv一个lid 代码 b锁 fences

**M是s在g C在tent**
- 参考s 到 镜像s 那个 d在't ex是t
- Incomplete secti在s
- TODO comments left 在

## 验证检查清单

- [ ] M一个rkdown 语法 是 v一个lid
- [ ] All l在ks w或k 和 参考 c或rect files
- [ ] He一个d在gs properly hier一个rchic一个l
- [ ] 代码 b锁s properly 对于m在ted
- [ ] No broken 镜像s
- [ ] No TODO comments
- [ ] 渲染s c或rectly
- [ ] File encod在g 是 UTF-8

## 相关技能
- **代码-re视图** - Re视图 document在i在 c在tent
- **file-一个一个lyzer** - F在d broken 镜像 files
